defaults:
  - /trainer@trainer: single
  - /generator@generator: greedy
  - _self_

_target_: pepo.model.chi2po.CHI2POModel
backbone: ${backbone}

alpha: 1.0
beta: 0.1
gamma: 1.0
r_max: 10.0
use_internal_clipping: true
debug: false

wandb:
  tags: ["chi2po"]

trainer:
  training_epochs: 1
