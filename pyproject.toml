[project]
name = "pepo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "datasets>=4.3.0",
    "hydra-core>=1.3.2",
    "peft>=0.17.1",
    "pyperf>=2.9.0",
    "torch>=2.8.0",
    "transformers>=4.57.1",
]

[dependency-groups]
dev = [
    "detect-secrets>=1.5.0",
    "pre-commit>=4.3.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
